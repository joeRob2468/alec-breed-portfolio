<div class="cell portfolio-item">
  <a href="{{ item.link }}">
    <img class="cover" src="{{ TimberImage(item.get_field('featured_image')).src|resize(775, 570) }}">
    <div class="hover-overlay">
      <p>{{ item.title }}</p>
    </div>
  </a>
</div>